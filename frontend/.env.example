# Leave unset in development: vite.config.ts proxies /api to localhost:3000.
# Set this for production builds when the API is not served from the same
# origin as the app, e.g.
# VITE_API_URL=https://api.example.com/api
