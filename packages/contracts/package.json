{
  "name": "@raven/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Wire format shared by the API and its clients. One definition, both sides.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
